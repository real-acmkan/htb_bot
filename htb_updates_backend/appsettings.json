{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Discord": {
    "ClientId": "", // client ID
    "ClientSecret": "", // client secret
    "RedirectUri": "" // change this to your domain hosting the bot. i.e "htb.example.com/callback" <- YOU MUST INCLUDE /callback
  },
  "JWT": {
    "ValidAudience": "*",
    "ValidIssuer": "http://localhost:5000",
    "Secret": "" // create a secret for signing JWT tokens
  },
  "ConnectionStrings": {
    "Default": "server=db;port=3306;database=htb;user=htb;password=<changeme>" // mysql creds from docker-compose.yml
  }
}